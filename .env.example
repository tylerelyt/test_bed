# ============================================
# AI Engineering Testbed - 环境变量配置示例
# ============================================
# 
# 使用说明：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 填入你的实际 API Key 和配置
# 3. .env 文件已被 .gitignore 忽略，不会被提交到版本控制
#
# ============================================

# ============================================
# LLM API 配置（必需 - 用于 NER、RAG、GUI Agent 等功能）
# ============================================

# 阿里云百炼 API Key（推荐，用于 Qwen 系列模型）
# 获取地址：https://help.aliyun.com/zh/model-studio/visual-reasoning
DASHSCOPE_API_KEY=your_dashscope_api_key_here

# OpenAI API Key（可选，用于 GPT 系列模型）
# 获取地址：https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Base URL（可选，默认使用阿里云百炼兼容模式）
# 如果使用 OpenAI 官方服务，设置为：https://api.openai.com/v1
# 如果使用阿里云百炼，设置为：https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# LLM 模型名称（可选，默认：qwen-plus）
# 支持的模型：qwen-plus, qwen-max, qwen-turbo, gpt-4o, gpt-4-turbo 等
LLM_MODEL=qwen-plus

# ============================================
# 可选配置
# ============================================

# Ollama 服务地址（可选，用于本地 LLM 推理）
# 默认：http://localhost:11434
# OLLAMA_URL=http://localhost:11434

# ============================================
# 注意事项
# ============================================
# 
# 1. 至少需要配置 DASHSCOPE_API_KEY 或 OPENAI_API_KEY 之一
# 2. 如果两个都配置，系统会优先使用 DASHSCOPE_API_KEY
# 3. 某些功能（如 GUI Agent、RAG）需要 API Key 才能正常工作
# 4. 如果不使用 LLM 功能，可以不配置 API Key（但相关功能将不可用）
#
